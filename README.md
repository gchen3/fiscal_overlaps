# Fiscal Overlaps Debt Map Comparison

This repository hosts a static Quarto/HTML comparison page for municipal debt maps.

Open `index.html` or `debt_map_comparison.html` to view:

- Massachusetts Reason Data vs Massachusetts DLS Data
- New York Reason Data vs New York OSC Data

The embedded map files are standalone HTML maps generated from the local analysis workflow.
